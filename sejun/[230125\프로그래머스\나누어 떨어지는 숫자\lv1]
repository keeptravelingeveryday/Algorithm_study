def solution(arr , divisor):
    return(sorted([i for i in arr if i % divisor == 0])) or [-1] 
#1. arr리스트에서 divisor로 나눠서 나머지가 0인 값(딱 떨어지는 값)을 리스트컴프리핸션으로 뽑고 sorted로 새로운 리스트를 오름차순으로 정렬해준다.
#2. 나머지가 0이 아닌 나머지를 else구문으로 구현하려고 했으나 구현하지 못하고 or 로 [-1]을 출력.
#3. python은 or 앞이 참일경우 뒷값까지 볼 필요가 없어 앞에 값까지만, 거짓일경우 뒤에 것까지 호출한다. 
#   즉, 나머지가 0이 아닐 때 앞의 sorted된 빈 리스트가 되므로 False가 되고 뒤에 [-1]이 호출된다. 
단락평가에 대한 자세한 예제와 설명 참조: https://marinelifeirony.tistory.com/83
